# 2026-03-31 To-Do

## To-Do

- [x] Create a shared dashboard shell-mode system with an explicit distinction between `page` and `workspace` sections.
- [x] Add tests for the shell-mode contract so layout decisions are not implicit or scattered.
- [x] Create a real dashboard section registry as the source of truth for section keys, titles, audiences, route scopes, navigation visibility, and shell mode.
- [x] Wire the live dashboard shells to the registry so section resolution is deterministic instead of hand-maintained in multiple places.
- [x] Apply the same registry-driven shell logic to the legacy mentee dashboard path so it cannot drift from the live shell.
- [x] Remove dead or unsupported sidebar navigation items that do not map to implemented sections.
- [x] Align root authenticated dashboard behavior with the implemented admin/mentor section surface where the UI currently exposes those sections.
- [x] Consolidate the remaining duplicate section rendering logic so there is one canonical dashboard shell implementation instead of overlapping shells.
- [x] Decide section-by-section which surfaces are true workspaces versus normal pages, with `sessions` as the next formal candidate.
- [x] Continue the tRPC migration by extracting messaging business logic into shared server services and retiring duplicate REST messaging handlers.
- [x] Implement real enforcement for `message_requests.maxMessages` / `messagesUsed` so request-backed conversations follow actual product rules.
- [x] Add unit tests for messaging service rules: send, accept/reject, archive, admin-direct bypass, and request message limits.
- [x] Add UI-level tests for dashboard workspace containment and message-thread scroll behavior.
- [x] Standardize dashboard route behavior so every section renders through the same contract with the same validation rules.

## Completed In This Pass

- Added the shared shell-mode/section registry foundation.
- Added unit tests for section resolution and shell mode.
- Migrated the live dashboard shell, root authenticated shell, and legacy mentee shell to the registry.
- Removed unsupported mentor sidebar entries (`earnings`, `settings`) and hid `home` from dashboard-route mentee navigation.
- Filled the missing root authenticated admin/mentor section handling that the sidebars already exposed.
- Replaced the overlapping root/dashboard section switches with one canonical authenticated dashboard experience and one shared route-url builder.
- Standardized mentor `schedule` rendering on the richer `MentorScheduleView` path instead of letting root/dashboard drift.
- Extracted messaging schemas, pure server rules, shared service functions, and shared HTTP error handling so tRPC and legacy REST adapters use the same domain logic.
- Enforced request-backed conversation follow-up limits through `message_requests.maxMessages/messagesUsed` for the requester side.
- Tightened the legacy message-request detail route so only participants can view it.
- Added unit tests for messaging server rules and UI-level tests for dashboard workspace framing plus fixed-header/fixed-composer message-thread layout.
