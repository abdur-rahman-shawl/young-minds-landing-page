# 2026-03-31 To-Do

## To-Do

- [x] Create a shared dashboard shell-mode system with an explicit distinction between `page` and `workspace` sections.
- [x] Add tests for the shell-mode contract so layout decisions are not implicit or scattered.
- [x] Create a real dashboard section registry as the source of truth for section keys, titles, audiences, route scopes, navigation visibility, and shell mode.
- [x] Wire the live dashboard shells to the registry so section resolution is deterministic instead of hand-maintained in multiple places.
- [x] Apply the same registry-driven shell logic to the legacy mentee dashboard path so it cannot drift from the live shell.
- [x] Remove dead or unsupported sidebar navigation items that do not map to implemented sections.
- [x] Align root authenticated dashboard behavior with the implemented admin/mentor section surface where the UI currently exposes those sections.
- [x] Consolidate the remaining duplicate section rendering logic so there is one canonical dashboard shell implementation instead of overlapping shells.
- [x] Decide section-by-section which surfaces are true workspaces versus normal pages, with `sessions` as the next formal candidate.
- [x] Continue the tRPC migration by extracting messaging business logic into shared server services and retiring duplicate REST messaging handlers.
- [x] Implement real enforcement for `message_requests.maxMessages` / `messagesUsed` so request-backed conversations follow actual product rules.
- [x] Add unit tests for messaging service rules: send, accept/reject, archive, admin-direct bypass, and request message limits.
- [x] Add UI-level tests for dashboard workspace containment and message-thread scroll behavior.
- [x] Standardize dashboard route behavior so every section renders through the same contract with the same validation rules.
- [x] Migrate notifications to shared server services and tRPC so the bell and internal notification emitters stop using route-local REST handlers.
- [x] Migrate the full bookings/session lifecycle to shared server services and tRPC, including booking, cancellation, rescheduling, no-show handling, reassignment, and session policy reads.
- [x] Move all live bookings/session UI callers off raw internal fetch calls and onto typed query/mutation hooks.
- [x] Delete the dead internal REST handlers for messaging, notifications, bookings, and session-policy reads once the final caller audit is clean.
- [x] Keep the policy-runtime guardrail aligned with the new shared service entry points instead of deleted route files.
- [x] Move admin session operations onto the shared bookings/session domain with explicit admin-only authorization checks and audit logging.
- [x] Expose admin session list, stats, detail, note, refund, cancel, complete, clear-no-show, and reassignment flows through tRPC.
- [x] Migrate the live admin sessions dashboard off raw internal fetch calls and onto typed query/mutation hooks.
- [x] Delete the dead internal admin session REST handlers after the final caller audit is clean.
- [x] Add regression tests for admin-only booking/session authorization and state-transition rules.
- [x] Migrate the mentor/admin content domain to a shared server service plus tRPC so content creation, course building, profile-content selection, and admin moderation stop using route-local REST handlers.
- [x] Move the live mentor content builder and admin content moderation UI off raw internal fetch calls and onto typed content query/mutation hooks.
- [x] Delete the dead internal mentor/admin content REST handlers after the caller audit confirms no remaining live consumers.
- [x] Tighten content workflow contracts while migrating: replace the stale `create_post_content` key, fix the render-time profile-content selector state sync bug, and remove the stale `PUBLISHED` edit-dialog status model.
- [x] Fix the content moderation/service drift exposed during the audit: include platform-owned content in admin moderation queries and make profile-content replacement transactional.
- [x] Migrate mentor, mentee, and admin analytics to shared server services and tRPC so the analytics surfaces stop depending on route-local server logic and ad hoc client fetches.
- [x] Gate analytics client-side with the real subscription feature contract so blocked plans do not spam protected analytics endpoints.
- [x] Remove the dead internal mentor/admin/mentee analytics REST handlers after the caller audit confirms no remaining live consumers.
- [x] Migrate subscription management to shared server services plus tRPC for authenticated self-subscription reads, plan selection, and admin plan/feature/pricing/analytics operations.
- [x] Move the live admin subscriptions dashboard and authenticated subscription UI callers off raw internal fetch calls and onto typed subscription query/mutation hooks.
- [x] Delete the dead internal admin subscription REST handlers and authenticated self-subscription REST handlers after the caller audit confirms no remaining live consumers.
- [x] Add regression tests for the new subscription service boundary, especially audience scoping, self-plan selection RBAC, and analytics input validation.
- [x] Migrate the remaining internal admin operations cluster for mentors, mentees, enquiries, overview metrics, and session-policy settings onto a shared admin service plus tRPC.
- [x] Move the live admin overview, mentor review, mentee directory, enquiries, and policy configuration UI off raw internal fetch calls and onto typed admin query/mutation hooks.
- [x] Delete the dead internal admin REST handlers for mentors, mentees, enquiries, mentor audit history, mentor coupons, and policy configuration after the caller audit confirms no remaining live consumers.
- [x] Add regression coverage for the admin mentor update rules, admin overview metrics contract, and admin policy grouping/default resolution.
- [x] Migrate the authenticated learning domain for enrolled courses, saved items, and session reviews to a shared server service plus tRPC so mentee learning and review flows stop using route-local REST handlers.
- [x] Move the live `My Learning`, `My Courses`, saved-items, and session-rating UI callers off raw internal fetch calls and onto typed learning query/mutation hooks.
- [x] Delete the dead internal student learning and review REST handlers after the caller audit confirms no remaining live consumers.
- [x] Add regression coverage for review audience resolution, weighted score calculation, and session-review contract behavior.
- [x] Fix the stale course publication contract so the public catalog, course detail visibility, and enrollment flow use the current `APPROVED` content status instead of the retired `PUBLISHED` value.
- [x] Migrate the mentor lifecycle domain as a hybrid: shared mentor service underneath, tRPC for authenticated JSON reads/mutations, and HTTP retained only for multipart application/profile upload flows.
- [x] Move the live mentor application status, mentor payment coupon validation, and mentor profile JSON update callers off raw internal fetch calls and onto typed mentor hooks.
- [x] Delete the dead internal JSON-only mentor lifecycle REST handlers once the final caller audit confirms no remaining live consumers.
- [x] Add regression coverage for the mentor coupon rules, mentor profile patch normalization, and the shared course visibility/enrollment status contract.
- [x] Migrate the current-user profile domain to a shared profile service plus tRPC so mentee profile editing stops depending on `/api/user/profile`.
- [x] Keep the public course catalog/detail and review reads on HTTP, while moving authenticated course enrollment, enrollment-status, progress, and review mutations onto the shared learning service plus tRPC.
- [x] Delete the dead authenticated profile/course REST handlers after the caller audit confirms no remaining live consumers, leaving the course review routes as public GET readers only.

## Completed In This Pass

- Added the shared shell-mode/section registry foundation.
- Added unit tests for section resolution and shell mode.
- Migrated the live dashboard shell, root authenticated shell, and legacy mentee shell to the registry.
- Removed unsupported mentor sidebar entries (`earnings`, `settings`) and hid `home` from dashboard-route mentee navigation.
- Filled the missing root authenticated admin/mentor section handling that the sidebars already exposed.
- Replaced the overlapping root/dashboard section switches with one canonical authenticated dashboard experience and one shared route-url builder.
- Standardized mentor `schedule` rendering on the richer `MentorScheduleView` path instead of letting root/dashboard drift.
- Extracted messaging schemas, pure server rules, shared service functions, and shared HTTP error handling so tRPC and legacy REST adapters use the same domain logic.
- Enforced request-backed conversation follow-up limits through `message_requests.maxMessages/messagesUsed` for the requester side.
- Tightened the legacy message-request detail route so only participants can view it.
- Added unit tests for messaging server rules and UI-level tests for dashboard workspace framing plus fixed-header/fixed-composer message-thread layout.
- Added shared notifications server schemas/services and a notifications tRPC router, then migrated the notification bell and internal notification emitters off `/api/notifications`.
- Added shared bookings/session server schemas/services and a bookings tRPC router covering list/detail/create/update, cancellation, rescheduling, no-show handling, reassignment, alternative mentor selection, and session policy reads.
- Migrated the live bookings/session UI callers to typed hooks across booking modal, session calendars, mentor schedule, cancellation/reschedule dialogs, reassignment flows, and the mentee select-mentor flow.
- Removed the dead internal notification routes, booking routes, and session-policy route after the caller audit confirmed no remaining non-doc consumers.
- Added a shared admin session service layer for admin list/stats/detail access, manual refund/cancel/complete/no-show clear flows, reassignment, and internal notes.
- Added admin-only tRPC procedures for the admin session surface and migrated the live admin sessions dashboard to typed React Query hooks.
- Removed the dead internal `/api/admin/sessions/**` REST handlers after the caller audit confirmed no remaining live callers.
- Added regression coverage for admin-only booking/session authorization and state-transition utilities.
- Added a shared content domain service plus a content tRPC router for mentor content CRUD, course metadata, module/section/item management, profile-content selection, and admin moderation.
- Migrated the live mentor content builder hooks and the admin moderation hooks off internal REST and onto typed tRPC clients, while keeping uploads on normal HTTP.
- Removed the dead internal mentor/admin content REST handlers after the caller audit confirmed no remaining live clients on `/api/mentors/content/**`, `/api/mentors/profile-content`, or `/api/admin/content/**`.
- Fixed the profile-content selector render-time state update bug, removed the stale `PUBLISHED` status-edit model from the mentor edit dialog, and corrected the file-upload field mapping used during content edits.
- Tightened the content service itself so admin moderation includes platform-owned content and profile-content replacement happens inside a database transaction.
- Added a shared analytics service plus an analytics tRPC router for mentor dashboard analytics, mentee learning analytics, and admin analytics.
- Migrated the live mentor analytics hook, mentee learning dashboard, and admin analytics page off internal analytics REST and onto typed tRPC query hooks.
- Added client-side analytics feature gating so mentors and mentees without analytics entitlements see explicit upgrade UI instead of hammering protected endpoints.
- Removed the dead internal analytics REST handlers after the caller audit confirmed no remaining live callers on `/api/analytics/mentor`, `/api/analytics/admin`, or `/api/student/learning-analytics`.
- Added a shared subscriptions service plus a subscriptions tRPC router for authenticated self-subscription reads, metered usage reads, plan selection, and admin subscription management operations.
- Migrated the live admin subscriptions surface, subscription display, course-access checks, and AI booking-rate lookup off internal subscription REST and onto typed subscription hooks.
- Removed the dead internal `/api/admin/subscriptions/**`, `/api/subscriptions/me`, `/api/subscriptions/me/usage`, and `/api/subscriptions/select` handlers after the caller audit confirmed no remaining live consumers.
- Added regression coverage for the shared subscription service, including self-subscription audience conflicts, plan-selection RBAC, admin override behavior, and analytics date validation.
- Added a shared admin service plus an admin tRPC router for overview metrics, mentor review/update flows, mentor coupon sending, mentor profile-audit reads, mentee directory reads, enquiries management, and session-policy management.
- Migrated the live admin overview, mentor review dashboard, mentee directory, enquiries inbox, policy configuration screen, and the legacy admin dashboard component off raw internal admin REST and onto typed admin hooks.
- Removed the dead internal `/api/admin/mentors/**`, `/api/admin/mentees`, `/api/admin/enquiries/**`, and `/api/admin/policies` handlers after the caller audit confirmed no remaining live consumers besides one unused backup file.
- Added regression coverage for the admin mentor update planner, admin overview metrics builder, and admin policy grouping/default-resolution helpers.
- Added a shared learning domain service plus a learning tRPC router for enrolled course listing, saved-items reads/removal, review-question reads, and session-review submission.
- Migrated the live `My Learning`, `My Courses`, saved-items, and `SessionRating` flows off internal REST and onto typed learning hooks, while correcting the stale `SessionViewModal` review contract to use the real `SessionRating` API.
- Preserved the saved-items rate limit when moving the domain off REST by making the limiter compatible with both Next request objects and tRPC request objects.
- Removed the dead internal `/api/student/courses`, `/api/student/saved-items`, `/api/reviews`, and `/api/reviews/questions` handlers after the caller audit confirmed no remaining live consumers.
- Added regression coverage for review role resolution, question-audience authorization, and weighted score calculation.
- Fixed the stale course publication contract by centralizing the public status on `APPROVED`, wiring the catalog/enrollment/detail flows to the shared helper, and preventing filtered catalog queries from silently dropping the publication guard.
- Added a shared mentor lifecycle service plus a mentor tRPC router for self-application reads, mentor profile JSON updates, and mentor coupon validation.
- Migrated the live mentor application screens, mentor payment gate, and mentor profile JSON update flows onto typed mentor hooks while retaining the multipart HTTP routes only for application submission and upload-backed profile updates.
- Removed the dead internal `/api/mentors/application` and `/api/mentor/payments/validate-coupon` handlers after the caller audit confirmed no remaining live consumers.
- Added regression coverage for mentor coupon validation rules, mentor profile patch normalization, and the shared course status helpers.
- Extended the mentor tRPC surface to cover mentor directory/detail reads, dashboard runtime reads, mentor-mentee session overview, course-comment replies, availability scheduling, availability exceptions, booking slot discovery, and booking eligibility checks.
- Rebased the remaining mounted legacy hook layer (`useMentorDetail`, `useSessions`, `useDashboardStats`, `useMentorMenteeSessions`) onto the shared tRPC/query layer so the UI contract stayed stable while the old fetch implementation disappeared.
- Extracted pure mentee dashboard/session derivation helpers and added regression coverage for dashboard summary/stat calculations plus legacy session filtering.
- Removed the dead internal mentor/session/dashboard REST handlers for mentor runtime reads, mentor availability/eligibility, mentor directory/detail, saved mentors, session helper reads, and dashboard stats after the final caller audit confirmed no remaining live consumers.
- Removed dead legacy hook/component files that only existed to call the deleted REST handlers, including the unused `time-slot-selector.tsx`, `use-mentor-mentees.ts`, and `use-user-roles.ts`.
- Added a shared profile service plus a profile tRPC router for authenticated current-user profile reads and mentee-profile upserts, then migrated the live mentee profile screen off `/api/user/profile`.
- Reworked the shared profile helper layer so it no longer hardcodes REST transport and now only carries reusable state/validation primitives.
- Extended the shared learning service plus `learning.*` tRPC router to cover course enrollment status, enrollment, course progress reads/updates, course-review submission/helpful voting, and content-item review submission.
- Migrated the live authenticated course surfaces (`CourseDetailView` enrollment/review actions and the `learn/[id]` progress/review flow) off internal REST and onto typed learning hooks while keeping public catalog/detail/review reads on HTTP.
- Removed the dead authenticated `/api/user/profile`, `/api/courses/[id]/enroll`, `/api/courses/[id]/progress`, and `/api/courses/[id]/reviews/[reviewId]/helpful` handlers, and trimmed the course/content-item review routes down to public GET-only readers.
- Updated the subscription-policy guardrail to track the shared learning service instead of the deleted course-enrollment route.
